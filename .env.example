# ============================================
# Career Bridge - Environment Configuration
# ============================================
# 
# IMPORTANT: Copy this file to .env and fill in the values
# DO NOT commit the actual .env file to version control
# 
# Command: cp .env.example .env

# ============================================
# SERVER (.env for /server)
# ============================================

# Port Configuration
PORT=5003

# Environment Mode
NODE_ENV=development

# Session & Security
SESSION_SECRET=your-random-session-secret-key

# MongoDB Atlas Connection
# Format: mongodb+srv://username:password@cluster.mongodb.net/database
MONGODB_URI=mongodb+srv://career-bridge:career-bridge%401234@career-bridge.7i4sjjw.mongodb.net/careerbridge?retryWrites=true&w=majority

# JWT Secret (used for token authentication)
JWT_SECRET=your-secret-jwt-key

# Email Configuration (Gmail)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-specific-password

# Google Gemini API (for AI roadmap generation)
# Get your key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-1.5-flash

# ML API URL (Python backend)
ML_API_URL=http://localhost:5002

# ============================================
# HYBRID ROADMAP (.env for /hybrid_roadmap)
# ============================================

# YouTube API Key (for resource recommendations)
# Get your key from: https://console.cloud.google.com/
YOUTUBE_API_KEY=your-youtube-api-key-here

# Google Gemini API (for skill subtopic generation)
# Must match the key in server/.env
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-1.5-flash

# ============================================
# CLIENT (.env for /client)
# ============================================

# API Base URL (should match server port)
VITE_API_URL=http://localhost:5003/api

# ============================================
# HOW TO GET REQUIRED KEYS
# ============================================

# 1. GEMINI_API_KEY (Required for AI features)
#    - Go to https://aistudio.google.com/apikey
#    - Click "Get API key"
#    - Click "Create API key in new project"
#    - Copy the key and paste here
#    - NOTE: Free tier has daily quota limits
#    - If you see 429 error: Enable billing in Google Cloud Console

# 2. YOUTUBE_API_KEY (Optional, for video recommendations)
#    - Go to https://console.cloud.google.com/
#    - Create new project
#    - Enable YouTube Data API v3
#    - Create API key credentials
#    - Copy and paste here

# 3. EMAIL_PASS (Gmail App Password)
#    - Go to https://myaccount.google.com/apppasswords
#    - Select Mail and Windows Computer
#    - Generate and copy the 16-character password
#    - Use that (not your Google password)

# 4. MONGODB_URI (Database connection)
#    - Already configured to Career Bridge cluster
#    - Contact admin if you need access

# ============================================
# TROUBLESHOOTING
# ============================================

# Issue: GEMINI_API_KEY returns 429 error (quota exhausted)
# Solution 1: Enable billing in Google Cloud Console (recommended)
# Solution 2: Create a new API key from https://aistudio.google.com/apikey
# Solution 3: Wait 24 hours for free tier quota to reset

# Issue: EMAIL_PASS authentication fails
# Solution: Use app-specific password, not your Google password
# Go to https://myaccount.google.com/apppasswords

# Issue: Can't connect to MongoDB
# Solution: Check MONGODB_URI format and ensure IP is whitelisted
# Whitelist your IP in MongoDB Atlas console

# Issue: ML API not responding
# Solution: Ensure hybrid_roadmap server is running on port 5002
# Command: cd hybrid_roadmap && python main.py
